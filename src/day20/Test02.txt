1.请简述UDP协议和TCP协议各自的特点。
UDP:数据发送端可接收端不必建立连接，发送端直接向指定的ip和端口发送数据，
不管接收端是否存在，接收端就算接收成功也不会提供反馈。
    消耗资源少，效率高，但是数据可能会丢包。不安全，数据包不能超过64k

TCP:由客户端发送连接请求，等待服务器确认，服务器发送一个响应，确认自己收到了
连接，可以建立连接，客户端收到响应，再次确认发送确认消息。经过这三次交互。
客户端和服务器就建立了连接。
    数据传输安全可靠，效率比UDP低效