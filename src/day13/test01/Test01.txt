1.栈和队列
 栈：
  又称堆栈，它是运算受限的线性表，
  其限制是仅允许在标的一端进行插入和删除操作，
  不允许在其 他任何位置进行添加、查找、删除等操作
        pop()弹栈：获取第一个元素并且，删除这个元素
         push()压栈：存入元素到第一个位置
   总结：只允许在头部的一端进行删除和插入，先进后出
队列：
    其限制是仅允许在表的一端进行插入， 而在表的另一端进行删除。
    总结：在头部只能插入，在尾部只能删除，先进先出
